"""Example models for the runtime.

Small models a reader can run to see the shape of the authoring surface and to
exercise the runtime standalone. Each is a ``reactor`` workspace. ``starter`` is
one class that writes ``generate()`` alone; ``echo`` and ``waypoint`` are split
into an application half and a model half. These live outside the published
package; they are illustrations, not part of the distribution.
"""
